apiVersion: v1
kind: Service
metadata:
  name: sportsclaw-relay
  namespace: sportsclaw
  labels:
    app: sportsclaw-relay
spec:
  type: ClusterIP
  selector:
    app: sportsclaw-relay
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
