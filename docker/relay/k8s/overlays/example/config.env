# =============================================================================
# SportsClaw Relay — User Configuration
#
# Copy this overlay folder for each user:
#   cp -r overlays/example overlays/<username>
#
# Then edit this file and kustomization.yaml with user-specific values.
# =============================================================================

# Active skills (comma-separated). Remove skills the user doesn't need.
# Available: football,nfl,nba,nhl,mlb,wnba,tennis,cfb,cbb,golf,f1,kalshi,polymarket,news,betting,markets
SPORTSCLAW_SKILLS=football,nba,nfl,betting

# LLM provider: anthropic | openai | google
SPORTSCLAW_PROVIDER=google

# Model override (leave empty for provider default)
# SPORTSCLAW_MODEL=gemini-2.0-flash

# Memory directory (inside the container — mapped to PVC)
SPORTSCLAW_MEMORY_DIR=/data/memory

# Relay HTTP port
RELAY_PORT=8080

# Python path (don't change unless you know what you're doing)
PYTHON_PATH=/opt/venv/bin/python3
